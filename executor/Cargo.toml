
[package]
name = "executor"
version = "0.1.0"
edition = "2021"

[dependencies]
fyrox = { workspace = true }
battlechisle = { path = "../game", optional = true }

[features]
default = ["battlechisle"]
dylib = ["fyrox/dylib"]
