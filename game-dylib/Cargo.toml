
[package]
name = "game_dylib"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
battlechisle = { path = "../game", default-features = false }

[features]
default = ["battlechisle/default"]
dylib-engine = ["battlechisle/dylib-engine"]
